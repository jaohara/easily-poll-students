# This "input" configures a global authorization rule to enable public access to
# all models in this schema. Learn more about authorization rules here: https://docs.amplify.aws/cli/graphql/authorization-rules
input AMPLIFY {
  globalAuthRule: AuthRule = { allow: public }
} # FOR TESTING ONLY!
type ColorSwatch @model {
  id: ID!
  color: String!
}

type Session @model {
  id: ID!
  questions: [Question] @hasMany
}

type Question @model {
  id: ID!
  prompt: String
}

# type ColorSwatch @model @auth(rules: [{ allow: public }]) {
#   id: ID!
#   color: String!
# }

# type Session
#   @model
#   @auth(
#     rules: [
#       { allow: public }
#       # { allow: public, operations: [read] }
#       # { allow: owner, operations: [update, delete] }
#       # { allow: public, operations: [create] }
#       # { allow: private, operations: [create] } ### restrict in the future
#     ]
#   ) {
#   id: ID!
#   questions: [Question] @hasMany
# }

# type Question
#   @model
#   @auth(
#     rules: [
#       { allow: public }
#       # { allow: public, operations: [read] }
#       # { allow: owner, operations: [update, delete] }
#       # { allow: public, operations: [create] }
#       # { allow: private, operations: [create] } ### restrict in the future
#     ]
#   ) {
#   id: ID!
#   prompt: String
# }
